# Task: Ví dụ Task File

## Objective
Đây là file ví dụ minh họa format task file. Worker đọc file này và thực thi.

## Instructions
1. Tạo file `src/greeter.ts` với function `greet(name: string): string` trả về `Hello, ${name}!`
2. Export function đó
3. Chạy `npm run build` để verify

## Acceptance Criteria
- [ ] `src/greeter.ts` tồn tại với đúng function signature
- [ ] Build không lỗi
- [ ] Không sửa file nào khác

## Files to Touch
- `src/greeter.ts` — tạo mới